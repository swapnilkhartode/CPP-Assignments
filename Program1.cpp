#include<iostream>
int main()
{
    std::cout<<"Good Morning\n";   // display output usingstd namespace but we required write every time std:: with every input and outout statement in code
    // so it is very complicted task,to solve this problem we requirwd import namespace std only once in top of program, so we need to use every time.
    std::cout<<"Good Afternoon\n";

    return 0;
}