#include<iostream>
class classname // we can strore different types of data in class as per our requirement(user defined datatype) it also known as complex data structure
{               // we can declare class only once and we can reuse its multiple times
                // we can reuse class more than one time by creating its object.
                // object is a block of memory where class data store or it is a instance of class
    access specifier;        // it is keyword which is used for apply restrictions on class and its member means it decide accesibility level of a class and its member
       datatype variablename;
       return type functionname(variables)
       {
          write here your logics
       }
};